from narad_mcp.server import mcp

if __name__ == "__main__":
    """
    Entry point for the Narad GitHub Agent.
    This file also ensures the directory structure displays clearly in the IDE.
    """
    mcp.run()
